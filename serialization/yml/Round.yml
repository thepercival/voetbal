Voetbal\Round:
  exclusion_policy: ALL
  exclude: false
  access_type: public_method
  custom_accessor_order: [number, config]

  properties:
    id:
      type: integer
    number:
      type: integer
      read_only: true
      accessor:
        getter: getNumberAsValue
      until_version: 1
    winnersOrLosers:
      type: integer
      serialized_name: winnersOrLosers
    qualifyOrder:
      type: integer
      serialized_name: qualifyOrder
    config:
      type: Voetbal\Round\Config
      until_version: 1
    childRounds:
      type: ArrayCollection<Voetbal\Round>
      serialized_name: childRounds
    poules:
      type: ArrayCollection<Voetbal\Poule>