Voetbal\Round:
  exclusion_policy: ALL
  exclude: false
  access_type: public_method
  custom_accessor_order: [number, config]

  properties:
    id:
      type: integer
    config:
      type: Voetbal\Round\Config
      until_version: 1
    poules:
      type: ArrayCollection<Voetbal\Poule>
    qualifyPoules:
      type: ArrayCollection<Voetbal\Qualify\Poule>