Voetbal\External\Team:
  type: entity
  repositoryClass: Voetbal\Team\External\Repository
  table: externalteams
  id:
    id:
      type: integer
      generator:
        strategy: AUTO
  fields:
    externalid:
      type: string
      length : !php/const:Voetbal\External\Object::MAX_LENGTH_EXTERNALID
      nullable: false
  manyToOne:
    externalsystem:
      targetEntity: Voetbal\External\System
      #inversedBy: teams
      joinColumn:
        name: externalsystemid
        referencedColumnName: id
        nullable: false
        onDelete: CASCADE
    importableobject:
      targetEntity: Voetbal\Team
      inversedBy: externals
      joinColumn:
        name: importableobjectid
        referencedColumnName: id
        nullable: false
        onDelete: CASCADE
  uniqueConstraints:
    main_idx:
      columns: [ externalsystemid, externalid ]