Voetbal\CountConfig\Score:
  type: entity
  repositoryClass: Voetbal\CountConfig\Score\Repository
  table: countscoreconfigs
  id:
    id:
      type: integer
      generator:
        strategy: AUTO
  fields:
    direction:
      type: smallint
      nullable: false
      unique: false
    maximum:
      type: smallint
      nullable: false
      unique: false
    iddep:
      type: integer
      nullable: true
      unique: false
    roundconfigiddep:
      type: integer
      nullable: true
      unique: false
  oneToOne:
    parent:
      targetEntity: Voetbal\CountConfig\Score
      inversedBy: child
      joinColumn:
        name: parentid
        referencedColumnName: id
        nullable: true
        onDelete: CASCADE
    child:
      targetEntity: Voetbal\CountConfig\Score
      mappedBy: parent
      cascade: [ persist, remove ]
  uniqueConstraints:
    number_u_idx:
      columns: [ parentid ]