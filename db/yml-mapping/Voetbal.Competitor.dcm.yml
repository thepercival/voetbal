Voetbal\Competitor:
  type: entity
  repositoryClass: Voetbal\Competitor\Repository
  table: teams
  id:
    id:
      type: integer
      generator:
        strategy: AUTO
  fields:
    name:
      type: string
      nullable: false
      length: !php/const Voetbal\Competitor::MAX_LENGTH_NAME
      unique: false
    abbreviation:
      type: string
      nullable: true
      unique: false
      length: !php/const Voetbal\Competitor::MAX_LENGTH_ABBREVIATION
      column: abbreviation
    info:
      type: string
      nullable: true
      length: !php/const Voetbal\Competitor::MAX_LENGTH_INFO
      unique: false
    imageUrl:
      type: string
      nullable: true
      length: !php/const Voetbal\Competitor::MAX_LENGTH_IMAGEURL
      unique: false
  oneToMany:
    externals:
      targetEntity: Voetbal\External\Competitor
      mappedBy: importableObject
  manyToOne:
    association:
      targetEntity: Voetbal\Association
      inversedBy: competitors
      joinColumn:
        name: associationid
        referencedColumnName: id
        nullable: false
        onDelete: CASCADE
