Voetbal\Config\Score:
  type: entity
  # update roundscoreconfigs set roundconfigid = (select id from roundconfigs where roundid = roundscoreconfigs.roundid )
  repositoryClass: Voetbal\Config\Score\Repository
  table: roundscoreconfigs
  id:
    id:
      type: integer
      generator:
        strategy: AUTO
  fields:
    name:
      type: string
      nullable: false
      length: !php/const Voetbal\Config\Score::MAX_LENGTH_NAME
      unique: false
    direction:
      type: smallint
      nullable: false
      unique: false
    maximum:
      type: smallint
      nullable: false
      unique: false
  manyToOne:
    configDep:
      targetEntity: Voetbal\Config
      inversedBy: scoresDep
      joinColumn:
        name: roundconfigid
        referencedColumnName: id
        nullable: true
        onDelete: CASCADE
  oneToOne:
    parent:
      targetEntity: Voetbal\Config\Score
      inversedBy: child
      joinColumn:
        name: parentid
        referencedColumnName: id
        nullable: true
        onDelete: CASCADE
    child:
      targetEntity: Voetbal\Config\Score
      mappedBy: parent
      cascade: [ persist, remove ]
  uniqueConstraints:
    number_u_idx:
      columns: [ parentid ]