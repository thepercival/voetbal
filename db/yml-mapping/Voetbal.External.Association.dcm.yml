Voetbal\External\Association:
  type: entity
  repositoryClass: Voetbal\External\Association\Repository
  table: externalassociations
  id:
    id:
      type: integer
      generator:
        strategy: AUTO
  fields:
    externalid:
      type: string
      length : !php/const:Voetbal\External\Object::MAX_LENGTH_EXTERNALID
      nullable: false
  manyToOne:
    externalsystem:
      targetEntity: Voetbal\External\System
      #inversedBy: associations
      joinColumn:
        name: externalsystemid
        referencedColumnName: id
        nullable: false
        onDelete: CASCADE
    importableobject:
      targetEntity: Voetbal\Association
      inversedBy: externals
      joinColumn:
        name: importableobjectid
        referencedColumnName: id
        nullable: false
        onDelete: CASCADE
  uniqueConstraints:
    main_idx:
      columns: [ externalsystemid, externalid ]