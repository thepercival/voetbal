Voetbal\Qualify\Group:
  type: entity
  repositoryClass: Voetbal\Qualify\Group\Repository
  table: qualifygroups
  id:
    id:
      type: integer
      generator:
        strategy: AUTO
  fields:
    winnersOrLosers:
      type: smallint
      nullable: false
      unique: false
    number:
      type: smallint
      nullable: false
      unique: false
      column: number
#    nrOfHorizontalPoules:
#      type: smallint
#      nullable: false
#      unique: false
  oneToOne:
    childRound:
      targetEntity: Voetbal\Round
      mappedBy: parentQualifyGroup
      joinColumn:
        name: childRoundId
        referencedColumnName: id
        nullable: true # should be false, do sql by hand
        onDelete: CASCADE
      # fetch: EAGER
      cascade: [ persist, remove ] # persist removed
  manyToOne:
    round:
      targetEntity: Voetbal\Round
      mappedBy: qualifyGroups
      joinColumn:
        name: roundid
        referencedColumnName: id
        nullable: false
        onDelete: CASCADE
  uniqueConstraints:
    number_u_idx:
      columns: [ roundid, winnersOrLosers, number ]